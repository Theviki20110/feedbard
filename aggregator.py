def aggregate_data(cleaned_text: str, removed_sections: list[dict]) -> str:
    # TODO: reinsert spoken markers for removed code/image/table sections
    # TODO: generate descriptions for removed sections
    # (design not finalized yet)
    return cleaned_text
